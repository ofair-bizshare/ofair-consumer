
import { RequestInterface, QuoteInterface } from '@/types/dashboard';

// Empty arrays as we'll load data from the database
export const requests: RequestInterface[] = [];
export const quotes: QuoteInterface[] = [];
